# ============================================================================
# tests/CMakeLists.txt — unit test build script
# ============================================================================
# Planned contents:
#   - Fetch/find test framework (Catch2 or GoogleTest)
#   - One test executable per module (or a single opre_tests target)
#   - Register with CTest via add_test()
# ============================================================================
